# ignore realm files
*.realm*

# Platform sources and binaries needed for Flutter plugin publishing
# Do NOT exclude iOS/Android/macOS/Windows/Linux plugin sources or xcframeworks.
# If you previously used symlinks, replace them with real files before publishing.
# Allow iOS xcframework and Android native sources to be included in the package.

# Keep excluding temporary build artifacts only (not plugin sources).
/windows/binary
/linux/binary

# Ignore realm-cre pem files
/ios/src/realm-core/**/*.pem

# Ignore the special tests folder for the flutter driver tests
tests/
